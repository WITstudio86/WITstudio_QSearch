
```rust
macro_rules! add {
    ($($x:expr), +) => {
        {
            let mut get_sum = 0;
            $(
                get_sum += $x;
            )+
            get_sum
        }
    };
}

macro_rules! add2 {
    ($($x:expr), +) => {
        {
            0
            $( + $x) +
        }
    };
}

fn main() {

}

#[cfg(test)]
mod test{
    #[test]
    fn test_add_macro() {
        assert_eq!(add!(1, 2, 3, 4, 5), 15);
        assert_eq!(add2!(1, 2, 3, 4, 5), 15);
    }
}
```
```rust
use num::PrimInt;
use std::convert::From;

#[derive(Debug)]
struct Number(i32);

impl <T:PrimInt> From<T> for Number {
    fn from(val : T) -> Self{
        Number(val.to_i32().unwrap())
    }
}
```

> `num = "0.4.3"` 